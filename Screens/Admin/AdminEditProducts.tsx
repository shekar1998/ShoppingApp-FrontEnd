import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

interface Props {
    
}

const AdminEditProducts = (props: Props) => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default AdminEditProducts

const styles = StyleSheet.create({})
